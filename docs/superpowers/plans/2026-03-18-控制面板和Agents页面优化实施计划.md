# 控制面板和 Agents 页面优化实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将控制面板（/dashboard）改为发现页面，我的 Agents（/agents）保持列表功能，所有统计数据使用真实的数据库查询结果。

**Architecture:**
- 后端创建统计服务层和路由，提供用户和全局统计数据接口
- 前端拆分 Dashboard 组件为两个独立页面（Discover + AgentsList），创建可复用的统计卡片组件
- 所有统计数据从数据库实时查询，移除模拟数据

**Tech Stack:** Python/FastAPI（后端）、TypeScript/React（前端）、SQLAlchemy（ORM）、SQLite（数据库）

---

## 任务概览

1. 后端开发：创建统计服务和路由
2. 前端开发：拆分页面组件和更新 API 层
3. 测试验证：确保功能正常

---

## Chunk 1: 后端统计服务开发

### Task 1: 创建统计服务层

**Files:**
- Create: `app/services/stats_service.py`
- Reference: `app/services/discover_service.py`（参考现有服务层模式）

- [ ] **Step 1: 创建统计服务文件**

```python
"""
统计服务层
提供用户和全局统计数据
"""
from typing import Dict
from sqlalchemy.orm import Session
from datetime import datetime, timedelta

from app.models.post import Post
from app.models.comment import Comment
from app.models.friendship import Friendship
from app.models.connected_agent import ConnectedAgent


async def get_user_stats(db: Session, user_id: str) -> Dict:
    """
    获取当前用户的统计数据

    Args:
        db: 数据库会话
        user_id: 用户ID

    Returns:
        dict: 包含总发帖数、总评论数、总好友数
    """
    # 统计用户的帖子数
    total_posts = db.query(Post).filter(
        Post.author_id == user_id,
        Post.is_deleted == False
    ).count()

    # 统计用户的评论数
    total_comments = db.query(Comment).filter(
        Comment.author_id == user_id,
        Comment.is_deleted == False
    ).count()

    # 统计用户的好友数
    total_friends = db.query(Friendship).filter(
        ((Friendship.user_id == user_id) | (Friendship.friend_id == user_id)),
        Friendship.status == 'accepted'
    ).count()

    return {
        "total_posts": total_posts,
        "total_comments": total_comments,
        "total_friends": total_friends
    }


async def get_global_stats(db: Session) -> Dict:
    """
    获取全局统计数据

    Args:
        db: 数据库会话

    Returns:
        dict: 包含全局统计数据
    """
    # 统计总帖子数
    total_posts = db.query(Post).filter(Post.is_deleted == False).count()

    # 统计总评论数
    total_comments = db.query(Comment).filter(Comment.is_deleted == False).count()

    # 统计总好友关系数
    total_friends = db.query(Friendship).filter(
        Friendship.status == 'accepted'
    ).count()

    # 统计活跃 Agent 数（最近24小时内有活动）
    twenty_four_hours_ago = datetime.utcnow() - timedelta(hours=24)

    active_agents = db.query(ConnectedAgent).filter(
        ConnectedAgent.is_active == True,
        ConnectedAgent.last_active_at >= twenty_four_hours_ago
    ).count()

    return {
        "total_posts": total_posts,
        "total_comments": total_comments,
        "total_friends": total_friends,
        "active_agents": active_agents
    }
```

- [ ] **Step 2: 保存文件**

将上述代码保存到 `app/services/stats_service.py`

- [ ] **Step 3: 提交更改**

```bash
git add app/services/stats_service.py
git commit -m "feat(stats): add stats service layer for user and global statistics"
```

---

### Task 2: 创建统计路由

**Files:**
- Create: `app/api/v1/stats.py`
- Reference: `app/api/v1/discover.py`（参考现有路由模式）

- [ ] **Step 1: 创建统计路由文件**

```python
"""
统计接口路由
提供：
- GET /api/v1/stats/user - 获取当前用户统计数据
- GET /api/v1/stats/global - 获取全局统计数据
"""
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session

from app.core.auth import get_current_user
from app.models.user import User
from app.database import get_db
from app.services.stats_service import (
    get_user_stats,
    get_global_stats
)

router = APIRouter(prefix="/stats", tags=["Stats"])


@router.get("/user")
async def get_current_user_stats(
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """
    获取当前用户统计数据

    需要认证：在 Header 中携带 JWT Token
    Authorization: Bearer {access_token}

    Returns:
        total_posts: 总发帖数
        total_comments: 总评论数
        total_friends: 总好友数
    """
    stats = await get_user_stats(db, current_user.user_id)

    return {
        "code": 0,
        "data": stats
    }


@router.get("/global")
async def get_site_global_stats(
    db: Session = Depends(get_db)
):
    """
    获取全局统计数据（网站概览）

    不需要认证（公开访问）

    Returns:
        total_posts: 总帖子数
        total_comments: 总评论数
        total_friends: 总好友数
        active_agents: 活跃 Agent 数
    """
    stats = await get_global_stats(db)

    return {
        "code": 0,
        "data": stats
    }
```

- [ ] **Step 2: 保存文件**

将上述代码保存到 `app/api/v1/stats.py`

- [ ] **Step 3: 提交更改**

```bash
git add app/api/v1/stats.py
git commit -m "feat(stats): add stats API endpoints for user and global statistics"
```

---

### Task 3: 注册统计路由到主应用

**Files:**
- Modify: `app/main.py`

- [ ] **Step 1: 读取当前的 app/main.py 文件**

```bash
cat app/main.py
```

- [ ] **Step 2: 在路由注册部分添加统计路由**

找到类似这样的代码块：
```python
from app.api.v1 import (
    auth,
    users,
    agents,
    posts,
    chat,
    friends,
    discover
)
```

修改为：
```python
from app.api.v1 import (
    auth,
    users,
    agents,
    posts,
    chat,
    friends,
    discover,
    stats  # 添加这一行
)
```

- [ ] **Step 3: 在应用初始化部分添加路由注册**

找到类似这样的代码：
```python
app.include_router(auth.router)
app.include_router(users.router)
app.include_router(agents.router)
app.include_router(posts.router)
app.include_router(chat.router)
app.include_router(friends.router)
app.include_router(discover.router)
```

在最后添加：
```python
app.include_router(stats.router, prefix="/api/v1", tags=["Stats"])
```

- [ ] **Step 4: 保存并提交**

```bash
git add app/main.py
git commit -m "feat(stats): register stats routes in main application"
```

---

### Task 4: 测试后端接口

- [ ] **Step 1: 启动后端服务**

```bash
cd D:\SocialClaw
poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

- [ ] **Step 2: 使用 curl 测试用户统计接口**

```bash
# 首先获取 access token（如果还没有）
# 然后测试：
curl -X GET "http://localhost:8000/api/v1/stats/user" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

**预期响应：**
```json
{
  "code": 0,
  "data": {
    "total_posts": 0,
    "total_comments": 0,
    "total_friends": 0
  }
}
```

- [ ] **Step 3: 测试全局统计接口**

```bash
curl -X GET "http://localhost:8000/api/v1/stats/global"
```

**预期响应：**
```json
{
  "code": 0,
  "data": {
    "total_posts": 0,
    "total_comments": 0,
    "total_friends": 0,
    "active_agents": 0
  }
}
```

- [ ] **Step 4: 手动插入测试数据并再次测试**

```sql
-- 插入测试帖子
INSERT INTO posts (post_id, author_id, content, created_at, updated_at)
VALUES
  ('post_1', 'your_user_id', '测试帖子1', datetime('now'), datetime('now')),
  ('post_2', 'your_user_id', '测试帖子2', datetime('now'), datetime('now'));

-- 插入测试评论
INSERT INTO comments (comment_id, post_id, author_id, content, created_at)
VALUES
  ('comment_1', 'post_1', 'your_user_id', '测试评论1', datetime('now'));

-- 再次测试，数据应该更新
```

- [ ] **Step 5: 确认测试通过后提交**

```bash
git commit -m "test(stats): verify stats endpoints work correctly"
```

---

## Chunk 2: 前端统计 API 和组件开发

### Task 5: 创建前端统计 API 层

**Files:**
- Create: `frontend/src/api/stats.ts`
- Reference: `frontend/src/api/agents.ts`（参考现有 API 模式）

- [ ] **Step 1: 创建统计 API 文件**

```typescript
import api from './auth';

/**
 * 获取用户统计数据
 */
export const getUserStats = async (): Promise<{
  totalPosts: number;
  totalComments: number;
  totalFriends: number;
}> => {
  const response = await api.get('/stats/user');

  if (response.data.code === 0) {
    return {
      totalPosts: response.data.data.total_posts,
      totalComments: response.data.data.total_comments,
      totalFriends: response.data.data.total_friends
    };
  }
  throw new Error(response.data.message || '获取统计数据失败');
};

/**
 * 获取全局统计数据
 */
export const getGlobalStats = async (): Promise<{
  totalPosts: number;
  totalComments: number;
  totalFriends: number;
  activeAgents?: number;
}> => {
  const response = await api.get('/stats/global');

  if (response.data.code === 0) {
    return {
      totalPosts: response.data.data.total_posts,
      totalComments: response.data.data.total_comments,
      totalFriends: response.data.data.total_friends,
      activeAgents: response.data.data.active_agents
    };
  }
  throw new Error(response.data.message || '获取全局数据失败');
};
```

- [ ] **Step 2: 保存文件**

将上述代码保存到 `frontend/src/api/stats.ts`

- [ ] **Step 3: 提交更改**

```bash
git add frontend/src/api/stats.ts
git commit -m "feat(stats): add frontend stats API layer"
```

---

### Task 6: 创建可复用的统计卡片组件

**Files:**
- Create: `frontend/src/components/StatsCard.tsx`
- Reference: `frontend/src/pages/Dashboard.tsx`（参考现有卡片样式）

- [ ] **Step 1: 创建统计卡片组件文件**

```typescript
import React from 'react';
import { LucideIcon } from 'lucide-react';

interface StatsCardProps {
  icon: LucideIcon;
  label: string;
  value: number | string;
  loading?: boolean;
  className?: string;
}

const StatsCard: React.FC<StatsCardProps> = ({
  icon: Icon,
  label,
  value,
  loading = false,
  className = ''
}) => {
  return (
    <div className={`p-6 bg-white dark:bg-slate-900 rounded-xl border border-primary/10 shadow-sm flex items-center gap-4 ${className}`}>
      <div className="size-12 rounded-lg bg-primary/10 flex items-center justify-center text-primary">
        <Icon className="size-6" />
      </div>
      <div>
        <p className="text-slate-500 text-sm font-medium">{label}</p>
        <p className="text-2xl font-bold">
          {loading ? '...' : typeof value === 'number' ? value.toLocaleString() : value}
        </p>
      </div>
    </div>
  );
};

export default StatsCard;
```

- [ ] **Step 2: 保存文件**

将上述代码保存到 `frontend/src/components/StatsCard.tsx`

- [ ] **Step 3: 提交更改**

```bash
git add frontend/src/components/StatsCard.tsx
git commit -m "feat(stats): add reusable StatsCard component"
```

---

### Task 7: 重构发现页面（原控制面板）

**Files:**
- Modify: `frontend/src/pages/Dashboard.tsx`（重命名为 Discover.tsx）

**注意：** 由于 Dashboard.tsx 当前被两个路由使用，我们需要：
1. 保留 Dashboard.tsx 用于 `/agents` 路由
2. 创建新的 Discover.tsx 用于 `/dashboard` 路由

- [ ] **Step 1: 读取当前 Dashboard.tsx**

```bash
cat frontend/src/pages/Dashboard.tsx
```

- [ ] **Step 2: 创建新的 Discover.tsx**

```typescript
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import { FileText, MessageSquare, UserPlus, Bell, LogOut, Info } from 'lucide-react';
import StatsCard from '../components/StatsCard';
import { getGlobalStats } from '../api/stats';

export default function Discover() {
  const [loading, setLoading] = useState(true);
  const [stats, setStats] = useState({
    totalPosts: 0,
    totalComments: 0,
    totalFriends: 0,
    activeAgents: 0
  });

  useEffect(() => {
    async function loadStats() {
      try {
        setLoading(true);
        const statsData = await getGlobalStats();
        setStats(statsData);
      } catch (error) {
        console.error('加载统计数据失败:', error);
        // 错误处理：显示错误消息或保持默认值
      } finally {
        setLoading(false);
      }
    }

    loadStats();
  }, []);

  return (
    <div className="flex-1 flex flex-col h-full bg-background-light dark:bg-background-dark">
      <header className="h-16 border-b border-primary/10 bg-white dark:bg-slate-900 flex items-center justify-between px-8 shrink-0">
        <div className="flex items-center gap-2">
          <span className="text-slate-400 text-sm">发现 /</span>
          <span className="text-sm font-medium">网站概览</span>
        </div>
        <div className="flex items-center gap-4">
          <button className="size-10 flex items-center justify-center rounded-full hover:bg-slate-100 dark:hover:bg-slate-800 text-slate-600">
            <Bell className="size-5" />
          </button>
          <Link to="/" className="flex items-center gap-2 px-4 py-2 rounded-lg bg-slate-100 dark:bg-slate-800 text-slate-700 dark:text-slate-300 font-bold text-sm hover:bg-slate-200 transition-colors">
            <LogOut className="size-4" />
            <span>退出登录</span>
          </Link>
        </div>
      </header>

      <div className="flex-1 overflow-y-auto p-8">
        <section className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
          <StatsCard
            icon={FileText}
            label="总发帖数"
            value={stats.totalPosts}
            loading={loading}
          />
          <StatsCard
            icon={MessageSquare}
            label="总评论数"
            value={stats.totalComments}
            loading={loading}
          />
          <StatsCard
            icon={UserPlus}
            label="总好友数"
            value={stats.totalFriends}
            loading={loading}
          />
          <StatsCard
            icon={Info}
            label="活跃 Agents"
            value={stats.activeAgents}
            loading={loading}
          />
        </section>

        {/* 后续可扩展：热门话题、推荐内容等 */}

        {loading && stats.totalPosts === 0 && (
          <div className="text-center py-12">
            <div className="inline-block animate-spin rounded-full h-12 w-12 border-b-2 border-primary"></div>
            <p className="mt-4 text-slate-500">正在加载数据...</p>
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 保存文件**

将上述代码保存到 `frontend/src/pages/Discover.tsx`

- [ ] **Step 4: 更新 App.tsx 路由**

读取 `frontend/src/App.tsx`，找到：
```typescript
import Dashboard from './pages/Dashboard';
```

修改为：
```typescript
import Dashboard from './pages/Dashboard';  // 保留用于 /agents
import Discover from './pages/Discover';    // 新增
```

然后找到路由配置：
```typescript
<Route path="/dashboard" element={<Dashboard />} />
<Route path="/agents" element={<Dashboard />} />
```

修改为：
```typescript
<Route path="/dashboard" element={<Discover />} />
<Route path="/agents" element={<Dashboard />} />
```

- [ ] **Step 5: 保存并提交**

```bash
git add frontend/src/pages/Discover.tsx frontend/src/App.tsx
git commit -m "feat(discover): create new Discover page for /dashboard route"
```

---

### Task 8: 更新 Agents 页面统计数据

**Files:**
- Modify: `frontend/src/pages/Dashboard.tsx`（现在仅用于 /agents 路由）

- [ ] **Step 1: 读取当前 Dashboard.tsx**

```bash
cat frontend/src/pages/Dashboard.tsx
```

- [ ] **Step 2: 修改组件名称和统计数据获取方式**

找到当前的模拟数据部分：
```typescript
const [stats, setStats] = useState({
  totalPosts: 1284,
  totalComments: 8432,
  totalFriends: 520
});
```

替换为真实数据获取：
```typescript
import { getUserStats } from '../api/stats';

const [stats, setStats] = useState({
  totalPosts: 0,
  totalComments: 0,
  totalFriends: 0
});
```

然后更新 useEffect 部分，添加统计数据加载：
```typescript
useEffect(() => {
  async function loadAgents() {
    try {
      setLoading(true);
      const agentsData = await getMyAgents();

      // 转换为前端格式
      const transformed = agentsData.map(agent => transformAgent(agent));
      setAgents(transformed);

      // 【新增】加载用户统计数据
      const statsData = await getUserStats();
      setStats(statsData);
    } catch (error) {
      console.error('加载 Agents 失败:', error);
      // ... 错误处理
    } finally {
      setLoading(false);
    }
  }

  loadAgents();
}, []);
```

- [ ] **Step 3: 保存并提交**

```bash
git add frontend/src/pages/Dashboard.tsx
git commit -m "feat(agents): use real stats data instead of mock data in Agents page"
```

---

### Task 9: 使用可复用的 StatsCard 组件（可选）

**Files:**
- Modify: `frontend/src/pages/Dashboard.tsx`

**注意：** 这一步是可选的优化，将硬编码的统计卡片替换为可复用组件。

- [ ] **Step 1: 修改 Dashboard.tsx 中的统计卡片部分**

找到：
```typescript
<section className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
  <div className="p-6 bg-white dark:bg-slate-900 rounded-xl border border-primary/10 shadow-sm flex items-center gap-4">
    <div className="size-12 rounded-lg bg-primary/10 flex items-center justify-center text-primary">
      <FileText className="size-6" />
    </div>
    <div>
      <p className="text-slate-500 text-sm font-medium">总发帖数</p>
      <p className="text-2xl font-bold">{loading ? '...' : stats.totalPosts.toLocaleString()}</p>
    </div>
  </div>
  {/* ... 其他两个卡片 */}
</section>
```

替换为：
```typescript
<section className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
  <StatsCard
    icon={FileText}
    label="总发帖数"
    value={stats.totalPosts}
    loading={loading}
  />
  <StatsCard
    icon={MessageSquare}
    label="总评论数"
    value={stats.totalComments}
    loading={loading}
  />
  <StatsCard
    icon={UserPlus}
    label="总好友数"
    value={stats.totalFriends}
    loading={loading}
  />
</section>
```

- [ ] **Step 2: 添加 StatsCard 导入**

在文件顶部添加：
```typescript
import StatsCard from '../components/StatsCard';
```

- [ ] **Step 3: 保存并提交**

```bash
git add frontend/src/pages/Dashboard.tsx
git commit -m "refactor(agents): use StatsCard component in Agents page"
```

---

## Chunk 3: 测试和验证

### Task 10: 前端页面测试

- [ ] **Step 1: 启动前端开发服务器**

```bash
cd frontend
npm run dev
```

- [ ] **Step 2: 访问发现页面（/dashboard）**

浏览器访问：`http://localhost:3000/dashboard`

**验证点：**
- 页面标题显示"发现 / 网站概览"
- 显示 4 个统计卡片（总发帖数、总评论数、总好友数、活跃 Agents）
- 数据是真实数据库数据（不是模拟数据）
- 页面无错误弹窗

- [ ] **Step 3: 访问我的 Agents 页面（/agents）**

浏览器访问：`http://localhost:3000/agents`

**验证点：**
- 页面标题显示"我的 Agents"
- 显示 3 个统计卡片（总发帖数、总评论数、总好友数）
- 显示 Agents 列表（如果有数据）
- 数据是真实数据库数据
- 页面无错误弹窗

- [ ] **Step 4: 对比两个页面**

**发现页面（/dashboard）：**
- 只有统计数据卡片
- 4 个卡片（全局数据）

**我的 Agents 页面（/agents）：**
- 统计数据卡片 + Agents 列表
- 3 个卡片（用户数据）

- [ ] **Step 5: 测试空数据状态**

如果没有统计数据：
- 页面显示加载动画
- 不显示错误弹窗
- 稍后显示默认值（0）

---

### Task 11: 集成测试

- [ ] **Step 1: 登录测试**

1. 访问登录页面
2. 点击"使用 Second Me 登录"
3. 授权后跳转到首页
4. 点击导航到"发现"或"我的 Agents"
5. 验证统计数据正常显示

- [ ] **Step 2: 数据一致性测试**

1. 在数据库中插入测试数据
2. 刷新页面
3. 验证前端显示的数据与数据库一致

- [ ] **Step 3: 错误处理测试**

1. 停止后端服务
2. 刷新前端页面
3. 验证错误处理（不应该崩溃，应该显示友好提示）

---

### Task 12: 性能测试

- [ ] **Step 1: 页面加载时间**

使用浏览器开发者工具：
1. 打开 Network 面板
2. 刷新页面
3. 记录页面加载时间和 API 响应时间
4. **预期：** 页面加载 < 2s，API 响应 < 500ms

- [ ] **Step 2: 大数据量测试**

1. 在数据库中插入大量测试数据（1000+ 帖子、评论）
2. 刷新页面
3. 验证查询性能和页面渲染速度

---

## 总结

完成以上所有任务后，将实现：

✅ 控制面板（/dashboard）改为发现页面，只显示全局统计数据
✅ 我的 Agents（/agents）保持列表功能，显示用户统计数据
✅ 所有统计数据使用真实的数据库查询（不再是模拟数据）
✅ 创建可复用的统计卡片组件
✅ 前后端接口完整对接

---

## 下一步

计划完成，可以开始执行实施计划了！
